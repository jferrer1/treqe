# Configuración Railway — Treqe Backend
# Docs: https://docs.railway.app/

[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile"
buildContext = "apps/api"

[service]
name = "treqe-api"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
